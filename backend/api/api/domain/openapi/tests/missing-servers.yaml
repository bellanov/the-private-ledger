openapi: 3.0.3
info:
  title: Missing Servers Example
  description: This spec violates the servers-required rule.
  version: 1.0.0
  contact:
    name: API Support
    email: support@example.com
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0.html

# Missing servers - violates servers-required (warn)

paths:
  /items:
    get:
      summary: List all items
      description: Returns a list of items.
      operationId: listItems
      tags:
        - items
      responses:
        "200":
          description: A list of items.
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
