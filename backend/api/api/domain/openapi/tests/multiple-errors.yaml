openapi: 3.0.3
info:
  title: Multiple Errors Example
  description: This spec violates multiple error-level rules at once.
  version: 1.0.0
  # Missing contact
  # Missing license

# Missing servers

paths:
  /items:
    get:
      # Missing summary - error
      description: Returns a list of items.
      # Missing operationId - error
      # Missing tags - warn
      responses:
        "200":
          description: A list of items.
    post:
      summary: Create an item
      operationId: createItem
      tags:
        - items
      # Missing responses - error
